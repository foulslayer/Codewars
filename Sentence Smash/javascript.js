function smash(words) {
  let test = words.join(" ");

  return test;
}

smash(["to", "a", "wa"]);

/*
function maps(x) {
    const newArr = x.map((item) => item * 2);
    return newArr;
  }
  
  console.log(maps([1, 2, 3])); // Output: [2, 4, 6]*/
